import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#179e8b",
    },
    secondary: {
      main: "#179e8b",
    },
    background: {
      default: "#FAF9F9",
    },
  },
});

export default theme;
